import requests
from bs4 import BeautifulSoup

# Replace with the URL of the weather website with the 10-day forecast
url = "https://www.wunderground.com/forecast/us/oh/youngstown"

# Send an HTTP GET request to fetch the webpage
response = requests.get(url)

# Parse the HTML content
soup = BeautifulSoup(response.content, 'html.parser')

#Locate and extract the weather data by inspecting the HTML structure
date = soup.find("div", class_="obs-date").text
#temperature = soup.find("div", class_="forecast").text
#conditions = soup.find("div", class_="obs-phrase").text

print(f"Date: {date}")
#print(f"Temperature: {temperature}")
#print(f"Conditions: {conditions}")